# ~/.bashrc — stub. Bash hardcodes this exact path for interactive
# shells (no $ZDOTDIR-style relocation like zsh has), so this file
# has to stay here. Everything else lives in ~/.bash/.
[ -f ~/.bash/bashrc ] && . ~/.bash/bashrc
